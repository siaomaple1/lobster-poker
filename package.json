{
  "name": "lobster-poker",
  "version": "1.0.0",
  "scripts": {
    "build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend && mkdir -p backend/public && cp -r frontend/dist/. backend/public",
    "start": "node backend/src/index.js"
  },
  "engines": {
    "node": ">=18"
  }
}
